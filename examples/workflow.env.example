# Overall model/tool workflow limits; the final round is for the answer only.
WORKFLOW_MAX_ROUNDS=8
WORKFLOW_DEADLINE_SECONDS=60
# Visual/document retrieval remains limited to three rounds and ten seconds.
# Exact-action approval has its own 60-second expiry; it does not extend retrieval.
